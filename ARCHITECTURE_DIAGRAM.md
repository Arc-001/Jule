# AI Template Manager - System Architecture

## File Structure Overview

```
Jule/
├── src/
│   ├── bot.py                              ← Loads template_manager cog
│   ├── cogs/
│   │   ├── template_manager.py            ← 🆕 Main implementation
│   │   ├── admin_commands.py              ← Has syncroles/syncchannels
│   │   └── help_commands.py               ← Updated with new commands
│   └── config/
│       ├── roles.yaml                     ← 🔒 PROTECTED (default)
│       ├── channels.yaml                  ← 🔒 PROTECTED (default)
│       ├── roles_generated_*.yaml         ← Generated templates
│       ├── channels_generated_*.yaml      ← Generated templates
│       ├── roles.json                     ← Role name → ID mappings
│       ├── current_channels.json          ← Channel name → ID mappings
│       └── backups/                       ← Automatic backups
│           ├── roles_20250110_120000.yaml
│           ├── roles_20250110_140000.yaml
│           ├── channels_20250110_120000.yaml
│           └── channels_20250110_140000.yaml
│
├── AI_TEMPLATE_MANAGER_GUIDE.md           ← 🆕 Complete guide
├── AI_TEMPLATE_MANAGER_QUICK_REF.md       ← 🆕 Quick reference
├── AI_TEMPLATE_MANAGER_IMPLEMENTATION.md  ← 🆕 Technical docs
├── TEMPLATE_MANAGER_SUMMARY.md            ← 🆕 This summary
└── test_template_manager.py               ← 🆕 Test suite

Legend: 🆕 New files | 🔒 Protected files
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Discord User                             │
│                    (Administrator Only)                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Commands: !genroles, !applyroles, etc.
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    TemplateManager Cog                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Command Handlers                                          │ │
│  │  • !genroles         • !applyroles     • !listtemplates   │ │
│  │  • !genchannels      • !applychannels  • !listbackups     │ │
│  │  • !reverttemplate                                        │ │
│  └───────────────────────┬────────────────────────────────────┘ │
│                          │                                       │
│  ┌───────────────────────┼────────────────────────────────────┐ │
│  │  Core Functions       │                                    │ │
│  │  ├─ _generate_roles_template()  ← AI Generation          │ │
│  │  ├─ _generate_channels_template()                        │ │
│  │  ├─ _create_backup()            ← Backup System          │ │
│  │  ├─ _list_backups()                                      │ │
│  │  ├─ _remove_managed_roles()     ← Cleanup               │ │
│  │  └─ _remove_managed_channels()                          │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────┬──────────────────────────────────────────┬────────┘
             │                                           │
             ▼                                           ▼
┌──────────────────────────┐               ┌──────────────────────┐
│   Google Gemini API      │               │  AdminCommands Cog   │
│   (AI Generation)        │               │  • syncroles()       │
│                          │               │  • syncchannels()    │
│   Model: gemini-2.0-     │               │                      │
│          flash-exp       │               │  (Used internally)   │
└──────────────────────────┘               └──────────┬───────────┘
                                                      │
                                                      ▼
                                           ┌──────────────────────┐
                                           │   Discord API        │
                                           │   • Create roles     │
                                           │   • Delete roles     │
                                           │   • Create channels  │
                                           │   • Delete channels  │
                                           └──────────────────────┘
```

## Data Flow: Template Generation

```
User Input: "Create roles for a gaming community..."
    │
    ▼
┌───────────────────────────────────────────────────┐
│ !genroles command handler                         │
│ • Validates user has admin permissions            │
│ • Shows "AI is generating..." status              │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ _generate_roles_template()                        │
│ • Builds structured prompt                        │
│ • Calls Gemini AI API                             │
│ • Parses and validates YAML response              │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Save to file                                      │
│ • Generate timestamp filename                     │
│ • Write to config/roles_generated_TIMESTAMP.yaml  │
│ • Count roles and categories                      │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Send success embed                                │
│ • Show template name                              │
│ • Show statistics (X roles, Y categories)         │
│ • Show preview of categories                      │
│ • Provide next steps: !applyroles command         │
└───────────────────────────────────────────────────┘
```

## Data Flow: Template Application

```
User Input: "!applyroles roles_generated_20250110_143022.yaml confirm"
    │
    ▼
┌───────────────────────────────────────────────────┐
│ Validation Checks                                 │
│ • Is file protected? (roles.yaml, channels.yaml)  │
│ • Does file exist?                                │
│ • Is "confirm" parameter present?                 │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Find Current Active Template                      │
│ • Check most recently modified template           │
│ • Exclude protected files                         │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Create Backup                                     │
│ • Copy current template to backups/               │
│ • Add timestamp to filename                       │
│ • Status: "💾 Backing up..."                      │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Remove Old Roles/Channels                         │
│ • Parse current template                          │
│ • Find matching Discord roles/channels            │
│ • Delete each (with 0.5s delay)                   │
│ • Skip @everyone and managed roles                │
│ • Status: "🗑️ Removing old..."                    │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Apply New Template                                │
│ • Call existing syncroles/syncchannels command    │
│ • Create new roles/channels                       │
│ • Update JSON mappings                            │
│ • Status: "✨ Applying new template..."           │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ Send Completion Embed                             │
│ • Show statistics (created/updated/found)         │
│ • List any errors                                 │
│ • Confirm backup location                         │
└───────────────────────────────────────────────────┘
```

## Safety Mechanisms

```
┌─────────────────────────────────────────────────────┐
│              Protected File Guard                    │
│  IF template_file IN ['roles.yaml', 'channels.yaml']│
│  THEN reject with error message                     │
│  ELSE proceed                                       │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│           Confirmation Requirement                   │
│  IF confirm != "confirm"                            │
│  THEN show warning embed with consequences          │
│  ELSE proceed                                       │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│            Automatic Backup                         │
│  BEFORE any destructive operation                   │
│  CREATE backup with timestamp                       │
│  STORE in config/backups/                           │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│            Rate Limiting                            │
│  AFTER each role/channel deletion                   │
│  WAIT 0.5 seconds                                   │
│  PREVENTS Discord API rate limits                   │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│         Error Handling & Logging                    │
│  TRACK statistics for each operation                │
│  LOG all errors with details                        │
│  REPORT to user in embed                            │
│  NEVER crash silently                               │
└─────────────────────────────────────────────────────┘
```

## Integration Points

```
┌────────────────────────────────────────────────────┐
│         TemplateManager Cog (New)                  │
└────────┬───────────────────────────────────┬───────┘
         │                                   │
         │ Calls                             │ Calls
         │                                   │
         ▼                                   ▼
┌──────────────────────┐        ┌────────────────────────┐
│ AdminCommands Cog    │        │  Google Gemini API     │
│ • syncroles()        │        │  • Template generation │
│ • syncchannels()     │        │  • Natural language    │
└──────────────────────┘        └────────────────────────┘
         │
         │ Updates
         │
         ▼
┌──────────────────────┐
│ RoleAssigner         │
│ • reload_role_       │
│   mappings()         │
└──────────────────────┘
```

## Command Permission Flow

```
User executes command
    │
    ▼
Discord.py checks @commands.has_permissions(administrator=True)
    │
    ├─ NO → Return "❌ You don't have permission"
    │
    └─ YES
        │
        ▼
    Command handler executes
        │
        ▼
    Discord API operations (Create/Delete roles/channels)
        │
        ├─ FORBIDDEN → Return "❌ Missing permissions to..."
        │
        └─ SUCCESS → Continue operation
```

## Backup System Design

```
Template Application Triggered
    │
    ▼
┌────────────────────────────────────────┐
│ Identify Current Active Template      │
│ (Most recently modified non-protected) │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ Generate Backup Filename               │
│ Format: {type}_{YYYYMMDD_HHMMSS}.yaml │
│ Example: roles_20250110_143022.yaml   │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ Copy to backups/ directory             │
│ Path: config/backups/{filename}        │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ Return backup path                     │
│ (For logging/confirmation)             │
└────────────────────────────────────────┘

Backups are:
• Never automatically deleted
• Timestamped for easy identification
• Listed via !listbackups command
• Restorable via !reverttemplate command
```

## Error Handling Strategy

```
┌────────────────────────────────────────┐
│ Every operation wrapped in try/except │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ Statistics tracking:                   │
│ • created: 0                           │
│ • updated: 0                           │
│ • deleted: 0                           │
│ • errors: []                           │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ On success: Increment counter          │
│ On failure: Append to errors list      │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│ Final embed shows:                     │
│ • All statistics                       │
│ • First 5 errors (with count)          │
│ • Color: Green (no errors) or Orange   │
└────────────────────────────────────────┘
```

## Performance Considerations

| Operation | Time | Notes |
|-----------|------|-------|
| AI Generation | 5-10s | Depends on Gemini API |
| Backup Creation | <1s | Simple file copy |
| Role Deletion | 0.5s each | Rate limited |
| Role Creation | 0.5s each | Rate limited |
| Channel Deletion | 0.5s each | Rate limited |
| Channel Creation | 0.5s each | Rate limited |

**Example**: Applying a template with 50 roles takes ~25 seconds (0.5s × 50)

## Security Model

```
┌─────────────────────────────────────────┐
│ Permission Layer 1: Discord Permissions │
│ • Only administrators can run commands   │
│ • Checked by @commands.has_permissions() │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│ Permission Layer 2: Protected Files     │
│ • roles.yaml and channels.yaml blocked  │
│ • Cannot be used in !apply* commands    │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│ Permission Layer 3: Confirmation        │
│ • Destructive ops require "confirm"     │
│ • Shows preview before execution        │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│ Permission Layer 4: Discord API         │
│ • Bot must have Manage Roles/Channels   │
│ • Bot role must be above target roles   │
└─────────────────────────────────────────┘
```

---

This architecture ensures:
- ✅ Safety through multiple layers
- ✅ Robustness through error handling
- ✅ Efficiency through async operations
- ✅ Usability through clear feedback
- ✅ Reliability through automatic backups

